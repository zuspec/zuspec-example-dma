
# Project initialization
- Use `ivpm update` to load project dependencies. You can run `uvx ivpm ...`

# Project operations
- Use IVPM to install dependencies. Edit ivpm.yaml to add new packages.
- Use the Python installation in packages/python

